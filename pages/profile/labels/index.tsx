import SettingsLayout from "@/components/layouts/SettingsLayout";
import "@aws-amplify/ui-react/styles.css";
import { profileNavItems } from "..";

const ProfileLabelPage = () => (
  <SettingsLayout title="Profile & Settings" sidebarNavItems={profileNavItems}>
    WORK IN PROGRESS
  </SettingsLayout>
);

export default ProfileLabelPage;
